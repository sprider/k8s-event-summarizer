apiVersion: v1
kind: Pod
metadata:
  name: test-pod
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      limits:
        memory: "64Mi"
        cpu: "100m"  
      requests:
        memory: "64Mi"  
        cpu: "100m" 
